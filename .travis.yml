language: php
php:
  - 7.0
  - 7.1
sudo: false
dist: trusty
before_script:
  - composer install
  - pecl update-channels; pecl install grpc-1.3.2
script:
  - vendor/bin/phpunit --configuration phpunit.xml.dist
matrix:
  fast_finish: true
